#ifndef BLECLIENT_HPP
#define BLECLIENT_HPP

#include <BleKeyboard.h>

// class BleClient : public BleKeyboard {
// public:
//   bool connectToServer;
//   BLEClient pClient;

// protected:
//   virtual void onStarted(BLEClient *pClient);
// };
#endif
